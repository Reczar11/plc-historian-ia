{services: {timescaledb: {image: timescale/timescaledb:latest-pg16, container_name: plc_timescaledb, restart: unless-stopped, environment: {POSTGRES_USER: "${POSTGRES_USER}", POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}", POSTGRES_DB: "${POSTGRES_DB}"}, ports: ["5432:5432"], volumes: ["timescale_data:/var/lib/postgresql/data", "./init-scripts:/docker-entrypoint-initdb.d"]}}, volumes: {timescale_data: null}}
